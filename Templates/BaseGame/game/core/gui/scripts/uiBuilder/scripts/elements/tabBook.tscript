function GuiFormBuilder::createTabBook()
{
   if(UIBuilderStack.count() == 0)
   {
      error("UIBuilder::createTabBook() - attempting to create element, but we have no containing control to add it to!");
      return false;
   }
   
   %baseCtrl = UIBuilderStack.getKey(UIBuilderStack.count()-1);
   
   %book = new GuiTabBookCtrl()
   {
      class = GuiFormBuilderTabBook;
      profile = ToolsGuiTabBookProfile;
      position = "0 0";
      extent = %baseCtrl.extent;
      internalName = "book";  
      horizSizing = "width";
      vertSizing = "height";
   };

   UIBuilderStack.push_back(%book);
   
   %baseCtrl.add(%book);
   
   return true;
}

function GuiFormBuilderTabBook::setAllowReorder(%this, %canReorder)
{
   %this.allowReorder = %canReorder;
}